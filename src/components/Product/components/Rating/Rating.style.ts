import { styled } from "@styled";

export const StyledProductRatingContainer = styled.div`
    display: inline-flex;
    align-items: center;
    font-size: 0;

    .MuiTypography-root {
        line-height: normal;
        margin: 4px 0 0 5px;
        font-size: 12px;
        color: #ffdf00;
    }

    svg {
        color: #ffdf00;
    }
`;
