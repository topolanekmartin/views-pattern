name: CI
on:
    push:
        branches:
            - main

jobs:
    deploy:
        name: 'Deploy'
        runs-on: 'ubuntu-latest'

        if: "!contains(github.event.head_commit.message,'#no-deploy')"
        steps:
            - uses: actions/checkout@v2

            - run: yarn install
            - run: yarn deploy
